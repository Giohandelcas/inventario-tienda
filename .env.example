# URL base de inventario-api (repo hermano). Sin prefijo NEXT_PUBLIC_: todo el
# fetching ocurre en Server Components/Actions, nunca en el cliente.
API_URL="http://localhost:3001"

# Firma la cookie de sesión local de cliente (lib/auth/session.ts). Generar con:
#   openssl rand -base64 32
SESSION_SECRET=""
